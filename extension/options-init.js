/* globals OptMan */
new OptMan().init({
	defaults: {
		actors: 'hover',
		starredRepos: 'group',
		forkedRepos: 'group',
		newRepos: 'group',
		collaborators: true,
		branches: true,
		commits: true,
		tags: true,
		loadMore: true,
	},
});
